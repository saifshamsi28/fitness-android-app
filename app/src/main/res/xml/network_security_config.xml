<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration

    This configuration allows cleartext (HTTP) traffic to localhost/emulator for development.

    WARNING: Remove or modify this for production builds!
-->
<network-security-config>

    <!-- Allow cleartext traffic to emulator localhost (10.0.2.2) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">0.0.0.0</domain>
        <domain includeSubdomains="true">10.163.19.205</domain>
    </domain-config>

    <!--
        For production, use base-config to enforce HTTPS:

        <base-config cleartextTrafficPermitted="false">
            <trust-anchors>
                <certificates src="system" />
            </trust-anchors>
        </base-config>
    -->

</network-security-config>